<?xml version="1.0" encoding="utf-8"?>
<!--
    Quick Action surface. Radius and border come from the shared card tokens so every gradient
    card in the app is geometrically identical to the flat ones; do not introduce a second
    radius here.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:startColor="@color/grad_mods_start"
                android:endColor="@color/grad_mods_end"
                android:angle="45" />
            <corners android:radius="@dimen/chimera_card_radius" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="@dimen/chimera_card_border_width"
                android:color="@color/card_border" />
            <corners android:radius="@dimen/chimera_card_radius" />
        </shape>
    </item>
</layer-list>
