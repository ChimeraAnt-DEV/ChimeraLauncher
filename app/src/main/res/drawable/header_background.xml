<?xml version="1.0" encoding="utf-8"?>
<!-- Deprecated alias: kept so existing layouts keep working. Points at the shared card
     tokens (@drawable/chimera_card); do not add new references, use Widget.Chimera.Card. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/chimera_card_radius" />
    <solid android:color="@color/surface" />
    <stroke
        android:width="@dimen/chimera_card_border_width"
        android:color="@color/card_border" />
</shape>
