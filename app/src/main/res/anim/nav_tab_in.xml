<?xml version="1.0" encoding="utf-8"?>
<!--
    Fade-through, incoming half. Tab switching swaps Activities, so there is no shared
    element to animate between them; a short fade plus a 0.92 -> 1.0 scale reads as the
    Material fade-through pattern and is what stops the swap feeling like a stock template.
-->
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="220"
    android:interpolator="@android:anim/decelerate_interpolator">
    <alpha
        android:fromAlpha="0.0"
        android:toAlpha="1.0" />
    <scale
        android:fromXScale="0.92"
        android:toXScale="1.0"
        android:fromYScale="0.92"
        android:toYScale="1.0"
        android:pivotX="50%"
        android:pivotY="50%" />
    <translate
        android:fromXDelta="2%p"
        android:toXDelta="0" />
</set>
